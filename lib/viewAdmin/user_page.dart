import 'package:flutter/material.dart';

class UserCRUDPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Manage Users'),
      ),
      body: Center(
        child: Text('User CRUD Page'),
      ),
    );
  }
}
